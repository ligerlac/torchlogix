use conda environment "torchlogix" to run this code
script my-aig-script.py outlines what functionality I want
I want to transform a circuit into an AND-inverter graph and save it to file so it can be read by 3rd party tools
We need to make sure that the AND-inverter graph is functionally equivalent to the original circuit "same input, same output" and test this
for a couple of randomly generated inputs

# open questions
